import React from 'react'

const Image = (props) => {
    return (
        <div className="image" style = "text-align:center">
            {props.children}
            
        </div>
    )
}

export default Image
